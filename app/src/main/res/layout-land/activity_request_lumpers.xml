<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.scheduleTime.RequestLumpersActivity">

    <include layout="@layout/layout_toolbar" />

    <include layout="@layout/content_request_lumpers" />

    <View
        android:id="@+id/bottomSheetBackgroundRequestLumpers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/fabBackgroundOverlay"
        android:visibility="gone" />

    <!-- Adding bottom sheet after main content -->
    <include layout="@layout/bottom_sheet_create_lumper_request" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>